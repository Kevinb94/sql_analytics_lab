# SQL Queries

- `dql/` contains analytical query examples.
- `ddl/` contains schema scripts. Use `pg_` prefix for PostgreSQL and `ch_` prefix for ClickHouse.
- `dml/` contains load/transform scripts. Use `pg_` prefix for PostgreSQL and `ch_` prefix for ClickHouse.
